{% extends 'base.html.twig' %}

{% block body %}

    <div class="my-3 p-3 bg-white rounded box-shadow mt-5 pt-5">

        {% for category in categories %}
            <a href="{{ path('categories.show', {'slug': category.slug}) }}">{{ category.name }}</a> <br>
        {% endfor %}
    </div>

{% endblock %}
