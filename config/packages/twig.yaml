twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    form_themes: ['bootstrap_4_layout.html.twig']
    globals:
        max_jobs_per_category: '%max_jobs_per_category%'
        jobs_upload_path: '/uploads/jobs/'
