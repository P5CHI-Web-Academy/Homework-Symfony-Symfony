twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    globals:
        jobs_show_limit_main_page: '%jobs_show_limit_main_page%'
        jobs_assets_directory: '/uploads/jobs'
    form_themes:
        - 'bootstrap_4_horizontal_layout.html.twig'
